spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/aproove-dbname
    username: aproove-dbuser
    password: aproove-dbpasswd
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
